node_modules/
.next/
dist/
.env
.env.local
.env*.local
*.log
.DS_Store

# Local uploads (never commit applicant/jury files)
backend/uploads/

# Spec Word docs at repo root (text copies live in _specs/ if you keep them)
*.docx

# OS / editor
Thumbs.db
*.swp
